/* all the headers previously defined */
struct headers {
    ethernet_t   ethernet;
    ipv4_t       ipv4;
    ports_t      ports;
    icmp_t       icmp;
    tcp_t        tcp;
    udp_t        udp;
}